{
  "name": "github-actions-training",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "test": "jest --coverage",
    "lint": "eslint src/ tests/",
    "build": "cp -r src dist"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.7.0"
  }
}